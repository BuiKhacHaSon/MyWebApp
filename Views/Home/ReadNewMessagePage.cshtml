@model IEnumerable<MyWebApp.Models.Message>

@{
    ViewData["Title"] = "ReadNewMessagePage";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>ReadNewMessagePage</h1>


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BodyMessage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsRead)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Agent)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Browser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Platform)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Device)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BodyMessage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsRead)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Agent)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Browser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Platform)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Device)
            </td>
            <td>
                <button class="btn btn-primary" onclick="Delete(@item.Id)">Xóa</button>|
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                <button class="btn btn-primary" onclick="CheckRead(@item.Id)">Read</button>|
            </td>
        </tr>
}
    </tbody>
</table>

<script>
    function Delete(id) {
        console.log(id);
        var dataJSON = {};
        $.ajax({
            type: 'DELETE',
            url: 'https://api.buikhachason.com/mes/' + id,
            dataType: 'json',
            data: JSON.stringify(dataJSON),
            contentType: "application/json",
            complete: function (jqXHR, status) {
                alert("Xóa được rồi đó ^^");
            },
            error: function (ex) {
                alert(ex.responseText);
            }
        });
    }
    function CheckRead(id) {
        console.log(id);
        var dataJSON = {};
        $.ajax({
            type: 'PUT',
            url: 'https://api.buikhachason.com/mes/' + id,
            dataType: 'json',
            data: JSON.stringify(dataJSON),
            contentType: "application/json",
            complete: function (jqXHR, status) {
                alert("Check được rồi đó ^^");
            },
            error: function (ex) {
                alert(ex.responseText);
            }
        });
    }
</script>
